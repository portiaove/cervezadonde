node_modules/
dist/
build/
.next/
coverage/
.turbo/

# env
.env
.env.local
.env.*.local
.env.prod
**/.env.prod

# deploy: uploaded web build + local dumps/archives
deploy/web-dist/
*.dump
*.tgz

# logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# analytics (GoAccess) — generated on the VPS, never committed
report.html
deploy/web-analytics/
deploy/geoip/
*.mmdb
# host-only Caddy routes (the /analytics password lives here, off the repo)
deploy/caddy-conf.d/

# editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# runtime data (root or any workspace subdir)
**/data/postgres/
**/data/raw/

# TypeScript incremental build info
*.tsbuildinfo
